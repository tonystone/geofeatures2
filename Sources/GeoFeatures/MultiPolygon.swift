///
///  MultiPolygon.swift
///
///  Copyright (c) 2016 Tony Stone
///
///  Licensed under the Apache License, Version 2.0 (the "License");
///  you may not use this file except in compliance with the License.
///  You may obtain a copy of the License at
///
///  http://www.apache.org/licenses/LICENSE-2.0
///
///  Unless required by applicable law or agreed to in writing, software
///  distributed under the License is distributed on an "AS IS" BASIS,
///  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
///  See the License for the specific language governing permissions and
///  limitations under the License.
///
///  Created by Tony Stone on 2/14/2016.
///

///
/// NOTE: This file was auto generated by gyb from file GeometryCollectionTypes.swift.gyb using the following command.
///
///     ~/gyb --line-directive '' -DSelf=MultiPolygon -DElement=Polygon -o MultiPolygon.swift GeometryCollectionTypes.swift.gyb
///
/// Do NOT edit this file directly as it will be regenerated automatically when needed.
///
import Swift

///
/// A MultiPolygon is a collection of some number of Polygon objects.
///
/// All the elements in a MultiPolygon will be converted to the same Spatial Reference System as `self`.
///
public struct MultiPolygon: Geometry {

    ///
    /// The `Precision` of this MultiPolygon
	///
    public let precision: Precision

    ///
    /// The `CoordinateSystem` of this MultiPolygon
	///
    public let coordinateSystem: CoordinateSystem

    ///
    /// Construct an empty `MultiPolygon`.
    ///
    public init() {
        self.init([], precision: defaultPrecision, coordinateSystem: defaultCoordinateSystem)
    }

    ///
    /// Construct an empty `MultiPolygon`.
    ///
    /// - Parameters:
    ///     - precision: The `Precision` model this `MultiPolygon` should use in calculations on it's coordinates.
    ///     - coordinateSystem: The 'CoordinateSystem` this `MultiPolygon` should use in calculations on it's coordinates.
	///
    public init(precision: Precision = defaultPrecision, coordinateSystem: CoordinateSystem = defaultCoordinateSystem) {
        self.init([] as [Polygon], precision: precision, coordinateSystem: coordinateSystem)
    }

    ///
    /// Construct a MultiPolygon from another MultiPolygon (copy constructor) changing the precision and coordinateSystem.
    ///
    /// - Parameters:
    ///     - other: The MultiPolygon of the same type that you want to construct a new MultiPolygon from.
    ///     - precision: Optionally change the `Precision` model this `MultiPolygon` should use in calculations on it's coordinates.
    ///     - coordinateSystem: Optionally change the 'CoordinateSystem` this `MultiPolygon` should use in calculations on it's coordinates.
	///
    public init(other: MultiPolygon, precision: Precision = defaultPrecision, coordinateSystem: CoordinateSystem = defaultCoordinateSystem) {
        self.init(other.elements, precision: precision, coordinateSystem: coordinateSystem)
    }

    ///
    /// Construct a `MultiPolygon` from an `Array` which holds `Polygon` objects.
    ///
    /// MultiPolygon can be constructed from any Swift.Collection type as
    /// long as it has an Element type equal the Geometry type specified in Element.
    ///
    /// - Parameters:
    ///     - elements: An `Array` of `Polygon`s.
    ///     - precision: The `Precision` model this `MultiPolygon` should use in calculations on it's coordinates.
    ///     - coordinateSystem: The 'CoordinateSystem` this `MultiPolygon` should use in calculations on it's coordinates.
	///
    public init(_ elements: [Polygon], precision: Precision = defaultPrecision, coordinateSystem: CoordinateSystem = defaultCoordinateSystem) {

        self.precision        = precision
        self.coordinateSystem = coordinateSystem

        /// Add the elements to our backing storage
        self.replaceSubrange(0..<0, with: elements)
    }

    private var elements: [Polygon] = []
}

// MARK: - ExpressibleByArrayLiteral conformance

extension MultiPolygon: ExpressibleByArrayLiteral {

    ///
    /// Creates an instance initialized with the given elements.
    ///
    /// - Parameters:
    ///     - elements: A variadic list of `Polygon`s.
    ///
    public init(arrayLiteral elements: Polygon...) {
        self.init(elements)
    }
}

// MARK: - `GeometryCollectionType` and `RangeReplaceableCollection` conformance.

extension MultiPolygon: GeometryCollectionType, RangeReplaceableCollection {

    ///
    /// Returns the position immediately after `i`.
    ///
    /// - Precondition: `(startIndex..<endIndex).contains(i)`
    ///
    public func index(after i: Int) -> Int {
        return i+1
    }

    ///
    /// Always zero, which is the index of the first element when non-empty.
    ///
    public var startIndex: Int {
       return 0
    }

    ///
    /// A "past-the-end" element index; the successor of the last valid subscript argument.
    ///
    public var endIndex: Int {
        return elements.count
    }

    ///
    /// Accesses the element at the specified position.
    ///
    /// - Parameters:
    ///     - index: The index of the polygon within the multi polygon.
    ///
    /// - Returns: The polygon at the specified position.
    ///
    public subscript(index: Int) -> Polygon {
        get {
            /// Note: we rely on the array to return an error for any index out of range.
            return elements[index]
        }
        set (newElement) {
            self.replaceSubrange(index..<(index + 1), with: [newElement])
        }
    }

    ///
    /// Replaces the specified subrange of elements with the given collection.
    ///
    /// This method has the effect of removing the specified range of elements
    /// from the collection and inserting the new elements at the same location.
    /// The number of new elements need not match the number of elements being
    /// removed.
    ///
    public mutating func replaceSubrange<C, R>(_ subrange: R, with newElements: C) where C : Collection, R : RangeExpression, Polygon == C.Element, Int == R.Bound {
        let preciseElements = newElements.map({ Polygon(other: $0, precision: self.precision, coordinateSystem: self.coordinateSystem) })

        self.elements.replaceSubrange(subrange, with: preciseElements)
    }
}

// MARK: CustomStringConvertible & CustomDebugStringConvertible protocol conformance

extension MultiPolygon: CustomStringConvertible, CustomDebugStringConvertible {

    public var description: String {
        return "\(type(of: self))([\(self.map { String(describing: $0) }.joined(separator: ", "))])"
    }

    public var debugDescription: String {
        return self.description
    }
}

// MARK: - Equatable Conformance

extension MultiPolygon: Equatable {
    static public func == (lhs: MultiPolygon, rhs: MultiPolygon) -> Bool {
        return lhs.elementsEqual(rhs, by: { (lhs2: Polygon, rhs2: Polygon) -> Bool in
            return lhs2 == rhs2
        })

    }
}
