///
///  MultiLineString.swift
///
///  Copyright (c) 2016 Tony Stone
///
///  Licensed under the Apache License, Version 2.0 (the "License");
///  you may not use this file except in compliance with the License.
///  You may obtain a copy of the License at
///
///  http://www.apache.org/licenses/LICENSE-2.0
///
///  Unless required by applicable law or agreed to in writing, software
///  distributed under the License is distributed on an "AS IS" BASIS,
///  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
///  See the License for the specific language governing permissions and
///  limitations under the License.
///
///  Created by Tony Stone on 2/14/2016.
///
import Swift

///
/// NOTE: This file was auto generated by gyb from file GeometryCollection.swift.gyb using the following command.
///
///     ~/gyb --line-directive '' -DSelf=MultiLineString -DElement=LineString<CoordinateType> -DCoordinateSpecialized=true -o MultiLineString.swift GeometryCollectionTypes.swift.gyb
///
/// Do NOT edit this file directly as it will be regenerated automatically when needed.
///

///
/// MultiLineString
///
/// A MultiLineString is a collection of some number of LineString<CoordinateType> objects.
///
/// All the elements in a MultiLineString shall be in the same Spatial Reference System. This is also the Spatial Reference System for the MultiLineString.
///
public struct MultiLineString<CoordinateType: Coordinate & CopyConstructable> {

    public let precision: Precision
    public let coordinateSystem: CoordinateSystem

    ///
    /// Construct an empty `MultiLineString`.
    ///
    /// - parameters:
    ///     - precision: The `Precision` model this `MultiLineString` should use in calculations on it's coordinates.
    ///     - coordinateSystem: The 'CoordinateSystem` this `MultiLineString` should use in calculations on it's coordinates.
    ///
    /// - seealso: `CoordinateSystem`
    /// - seealso: `Precision`
    ///
    public init(precision: Precision = defaultPrecision, coordinateSystem: CoordinateSystem = defaultCoordinateSystem) {
        self.precision        = precision
        self.coordinateSystem = coordinateSystem
        self.elements         = []
    }

    ///
    /// Construct a MultiLineString from another MultiLineString (copy constructor).
    ///
    /// - parameters:
    ///     - other: The MultiLineString of the same type that you want to construct a new MultiLineString from.
    ///
    public init(other: MultiLineString<CoordinateType>) {

        self.precision        = other.precision
        self.coordinateSystem = other.coordinateSystem
        self.elements         = other.elements
    }

    ///
    /// Construct a MultiLineString from another MultiLineString (copy constructor) changing the precision and coordinateSystem.
    ///
    /// - parameters:
    ///     - other: The MultiLineString of the same type that you want to construct a new MultiLineString from.
    ///     - precision: Optionally change the `Precision` model this `MultiLineString` should use in calculations on it's coordinates.
    ///     - coordinateSystem: Optionally change the 'CoordinateSystem` this `MultiLineString` should use in calculations on it's coordinates.
    ///
    /// - seealso: `CoordinateSystem`
    /// - seealso: `Precision`
    ///
    internal init(other: MultiLineString<CoordinateType>, precision: Precision, coordinateSystem: CoordinateSystem) {

        self.precision        = precision
        self.coordinateSystem = coordinateSystem

        /// We create a new instance of the LineString<CoordinateType> so we can adjust the precision and Coordinate reference system of the LineString<CoordinateType> before adding.
        self.elements         = other.elements.map({ LineString<CoordinateType>(other: $0, precision: precision, coordinateSystem: coordinateSystem) })
    }

    ///
    /// Construct a `MultiLineString` from a an `Array` of `LineString<CoordinateType>`.
    ///
    /// - parameters:
    ///     - elements: An `Array` of `LineString<CoordinateType>` coordinates.
    ///     - precision: The `Precision` model this `MultiLineString` should use in calculations on it's coordinates.
    ///     - coordinateSystem: The 'CoordinateSystem` this `MultiLineString` should use in calculations on it's coordinates.
    ///
    /// - seealso: `CoordinateSystem`
    /// - seealso: `Precision`
    ///
    public init(elements: [LineString<CoordinateType>], precision: Precision = defaultPrecision, coordinateSystem: CoordinateSystem = defaultCoordinateSystem) {

        self.precision        = precision
        self.coordinateSystem = coordinateSystem

        /// We create a new instance of the LineString<CoordinateType> so we can adjust the precision and Coordinate reference system of the LineString<CoordinateType> before adding.
        self.elements         = elements.map({ LineString<CoordinateType>(other: $0, precision: precision, coordinateSystem: coordinateSystem) })
    }

    ///
    /// Construct a `MultiLineString` from a any `Collection` type which holds `Element` objects.
    ///
    /// MultiLineString can be constructed from any Swift.Collection type as
    /// long as it has an Element type equal the Geometry type specified in Element.
    ///
    /// - parameters:
    ///     - elements: A `Collection` of `Element`s.
    ///     - precision: The `Precision` model this `MultiLineString` should use in calculations on it's coordinates.
    ///     - coordinateSystem: The 'CoordinateSystem` this `MultiLineString` should use in calculations on it's coordinates.
    ///
    /// - seealso: `CoordinateSystem`
    /// - seealso: `Precision`
    ///
    public init<C: Swift.Collection>(elements: C, precision: Precision = defaultPrecision, coordinateSystem: CoordinateSystem = defaultCoordinateSystem)
            where C.Iterator.Element == LineString<CoordinateType> {

        self.precision = precision
        self.coordinateSystem = coordinateSystem

        /// We create a new instance of the LineString<CoordinateType> so we can adjust the precision and Coordinate reference system of the LineString<CoordinateType> before adding.
        self.elements = elements.map({ LineString<CoordinateType>(other: $0, precision: precision, coordinateSystem: coordinateSystem) })
    }

    internal private(set) var elements: [LineString<CoordinateType>]
}

// MARK: - Collection conformance

extension MultiLineString: Collection, MutableCollection {

    ///
    /// Returns the position immediately after `i`.
    ///
    /// - Precondition: `(startIndex..<endIndex).contains(i)`
    ///
    public func index(after i: Int) -> Int {
        return i+1
    }

    ///
    /// Always zero, which is the index of the first element when non-empty.
    ///
    public var startIndex: Int {
       return 0
    }

    ///
    /// A "past-the-end" element index; the successor of the last valid subscript argument.
    ///
    public var endIndex: Int {
        return elements.count
    }

    public subscript(index: Int) -> LineString<CoordinateType> {
        get {
            /// Note: we rely on the array to return an error for any index out of range.
            return elements[index]
        }
        set (newElement) {
            /// Note: we rely on the array to return an error for any index out of range.
            /// We create a new instance of the LineString<CoordinateType> so we can adjust the precision and Coordinate reference system of the LineString<CoordinateType> before adding.
            self.elements[index] = LineString<CoordinateType>(other: newElement, precision: self.precision, coordinateSystem: self.coordinateSystem)
        }
    }

    ///
    /// Append `newElement` to MultiLineString.
    ///
    public mutating func append(_ newElement: LineString<CoordinateType>) {

        /// We create a new instance of the LineString<CoordinateType> so we can adjust the precision and Coordinate reference system of the LineString<CoordinateType> before adding.
        self.elements.append(LineString<CoordinateType>(other: newElement, precision: self.precision, coordinateSystem: self.coordinateSystem))
    }

    ///
    /// Append the elements of `newElements` to this MultiLineString.
    ///
    public mutating func append<C: Swift.Collection>(contentsOf newElements: C)
            where C.Iterator.Element == LineString<CoordinateType> {

        var Iterator = newElements.makeIterator()

        while let element = Iterator.next() {
            self.append(element)
        }
    }

    ///
    /// Insert `newElement` at index `i` of this MultiLineString.
    ///
    /// - Requires: `i <= count`.
    ///
    public mutating func insert(_ newElement: LineString<CoordinateType>, at index: Int) {

        /// We create a new instance of the LineString<CoordinateType> so we can adjust the precision and Coordinate reference system of the LineString<CoordinateType> before adding.
        self.elements.insert(LineString<CoordinateType>(other: newElement, precision: self.precision, coordinateSystem: self.coordinateSystem), at: index)
    }
}

// MARK: CustomStringConvertible & CustomDebugStringConvertible protocol conformance

extension MultiLineString: CustomStringConvertible, CustomDebugStringConvertible {

    public var description: String {
        return "\(type(of: self))(\(self.map { String(describing: $0) }.joined(separator: ", ")))"
    }

    public var debugDescription: String {
        return self.description
    }
}

// MARK: - Equatable Conformance

extension MultiLineString: Equatable {}

public func == <CoordinateType>(lhs: MultiLineString<CoordinateType>, rhs: MultiLineString<CoordinateType>) -> Bool {
    return lhs.equals(rhs)
}
